using UnityEngine;

namespace Team.Enum.Character
{
    public enum Enum_CharacterState
    {
        Alive,
        Dead,
        Incapacitated
    }

    public enum Enum_ProjectileType
    {
        Fireball,
        NonLethalRound
    }

    public enum Enum_charGridDirection
    {
        NORTH,
        EAST,
        SOUTH,
        WEST
    }
}
